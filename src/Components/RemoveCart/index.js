/*const RemoveCart = ({ currentSale, setCurrentSale, idProduto }) => {
  setCurrentSale(currentSale.filter((product) => product.id !== idProduto));
  console.log(idProduto);
};

export default RemoveCart;
*/
